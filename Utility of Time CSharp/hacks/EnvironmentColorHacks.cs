namespace UoT {
  /// <summary>
  ///   Hack class for setting specific environment colors for given models.
  /// </summary>
  public class EnvironmentColorHacks {
    // TODO: Add a tab for these toggles.
    // TODO: Add toggle for Link's tunic.

    /**
     *     ShaderManager.SetEnvironmentColor(Tunic.TUNIC_COLOR_KOKIRI(0) / 255.0F,
                                      Tunic.TUNIC_COLOR_KOKIRI(1) / 255.0F,
                                      Tunic.TUNIC_COLOR_KOKIRI(2) / 255.0F,
                                      Tunic.TUNIC_COLOR_KOKIRI(3) / 255.0F)
     */
  }
}
